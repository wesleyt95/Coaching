<%= form_with(model: add_time_to_appointment, local: true) do |form| %>
  <% if add_time_to_appointment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(add_time_to_appointment.errors.count, "error") %> prohibited this add_time_to_appointment from being saved:</h2>

      <ul>
      <% add_time_to_appointment.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :time %>
    <%= form.time_select :time, id: :add_time_to_appointment_time %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
